export const test = "test";
// import { css } from "@emotion/css";

// export function createStyles(obj) {
//   return Object.keys(obj).reduce((memo, val) => {
//     memo[val] = css(obj[val]);
//     return memo;
//   }, {});
// }
