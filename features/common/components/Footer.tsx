import { css } from "@emotion/css";

function Footer() {
  return (
    <div className={styles.wrapper}>
      <div className="">isi</div>
      <div>isi</div>
    </div>
  );
}

export default Footer;

const styles = {
  wrapper: css({
    backgroundColor: "#3eb7a4",
    paddingBlock: 30,
    flexDirection: "column",
  }),
};
